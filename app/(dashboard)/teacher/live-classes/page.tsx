import {
    ZoomGeneralDashboard,
} from "@/components/live-classes/ZoomGeneralDashboard";

export default function TeacherLiveClassesPage() {
    return (
        <ZoomGeneralDashboard audience="teacher" />
    );
}